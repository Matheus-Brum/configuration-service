{
  "name": "configuration-service",
  "version": "1.0.0",
  "description": "service that manages and cetralizes external services configuration",
  "main": "src/app.ts",
  "scripts": {
    "start": "node dist/app.js",
    "prestart": "npm run build",
    "build" : "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Matheus Brum",
  "dependencies": {
    "@types/cors": "^2.8.5",
    "@types/express": "^4.17.0",
    "cors": "^2.8.5",
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "typescript": "^3.5.3"
  }
}
